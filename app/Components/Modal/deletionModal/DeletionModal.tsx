//삭제 버튼을 누르면 정말 삭제하시겠습니까??? 하고 물어보는 모달, 글쓴이와 현재 유저를 비교하는 함수 들어가 있어야함,둘다 일치할시 whenClicked로 들어오는 패치 함수 돌람

import Modal from '../Modal';

export default function DeletionModal({whenClicked}:any){
    return(
        
        <Modal >
    
            <div>

                sdafdsaf
            </div>
            </Modal>
        
    )

}